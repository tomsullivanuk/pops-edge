# MLB simple-report presentation correction — September 13, 2026

The Product Owner rejected the technical export as the principal experience and
approved technical evidence under **Details**, with a simple report generated from
already acquired data. This corrects the current uncommitted delivery candidate
before independent implementation review; prior PDF rendering checks did not
establish product usability.

The principal screen is the live-quote Performance Report with a small independent
Historical candle report link. It shows friendly study/report dates and timezone,
incomplete status, eligible/captured/scored counts, material missing/awaiting-result
counts, rounded Brier versus the constant 50% reference on the identical scored set,
and governed uncertainty/sample cautions. Lower Brier is better; zero means no error.
No percentage-accuracy substitute, significance claim or betting implication is added.

Full identifiers/hashes/JSON/Protocol and source references, internal timestamps,
exact values, log loss, detailed calibration, bounded results, complete failure
categories and reproduction/download links belong in collapsed Details. Material
absence, incomplete-study and failed-update notices remain visible. Rounding is
presentation only: canonical populations, scores, seeds, dates and bytes do not change.
Historical output follows the same hierarchy and retains its fixed separate scope.

Use a new renderer version; existing immutable packages remain unchanged and their
original renderer remains available for verification. A saved local principal entry
may project the current validated package without acquiring or rescoring anything.
No shared shell, server, new statistic, source repair or automatic refresh is added.

Local presentation/tests/docs, read-only discovery and verification of the acquired
MLB archive, and local report generation from valid acquired Evidence are authorized.
Outputs belong outside source checkouts and archive roots. The clean explicit code
revision prerequisite remains mandatory; never use the synthetic seam on acquired
data. Complete correction and safe source-readiness work before reporting the
minimum remaining provenance/integration prerequisite. Commit, push/PR/merge,
deployment, new acquisition, collector/scheduler or Evidence/index/checkpoint mutation,
study closure and release remain unauthorized. Independent review is deferred until
the correction is ready, and is not delegated by this decision.

This decision amends presentation/delivery scope under Product and Architecture;
it does not change Methodology, original scientific contracts or protocol closure.
