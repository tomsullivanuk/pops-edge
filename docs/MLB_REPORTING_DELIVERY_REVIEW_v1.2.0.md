# PM / Chief Architect review — archive-to-report delivery

> Historical delivery receipt: the Owner subsequently required a simple principal
> report and authorized acquired-data reads/local generation. The current candidate
> and disposition are in [the correction receipt](MLB_SIMPLE_REPORT_IMPLEMENTATION_v1.2.0.md)
> and [PM self-review](MLB_SIMPLE_REPORT_REVIEW_v1.2.0.md). Earlier evidence below
> remains historical; it does not override the later usability correction.

September 13, 2026. **Ready for independent implementation acceptance review:
automated self-validation passes and the representative basic-export visual
requirement is satisfied.** No concrete P0/P1 implementation or visual defect was
identified in this bounded self-review and incorporated visual evidence. Independent
implementation acceptance has not been performed; it is the next gate.

This is a separate PM/Chief Architect assessment by the implementation author,
**not an independent review**. It does not accept the complete delivery gate,
production reports, integration, deployment, either study's closure or v1.2 release.
The [implementation receipt](MLB_REPORTING_DELIVERY_IMPLEMENTATION_v1.2.0.md),
[exact patch](../planning/DELIVERY_CANDIDATE.patch) and
[file hashes](../planning/DELIVERY_FILE_INVENTORY.json) identify the candidate.
The [remaining validation handoff](../planning/DELIVERY_VALIDATION_HANDOFF.md) is
now scoped to independent implementation acceptance; it is not a deployment/integration prompt.

## Scope and governing boundaries

Base/HEAD is `3001ee9cd4d95519567e0e4567ff71be0465a6a0` (PR #50), freshly
fetched at initiation, first parent `a658233dd218638025232630540c8044afb65a35`.
No candidate commit exists. The initial worktree was clean. Applied complete root
AGENTS.md and Methodology > Product > Architecture > release decisions/contract >
implementation, with the complete authorized archive-to-report handoff and its
companion planning review. The earlier slices' acceptance is a dependency only.

Must hold: separately retained trusted source authority; exact graph/root/projection
replay; immutable source and earlier packages; truthful K/computation/report/delivery
chronology; exact separate-study populations, reference and fixed-bin statistics;
useful saved output; preserved historical selection and visible safe failures.
Accepted limits remain local/manual execution, retained source/anchor prerequisites,
incomplete calendar/studies, insufficient samples, missed acquisition, frozen
presentation, trusted local filesystem/clocks and documented manual recovery.
Automatic recovery, final/correction publication, polished reader, shared shell,
provider access, operational acquisition, Governance and wagering are excluded.

## Assessment against must-hold conditions

| Boundary | Self-review result |
| --- | --- |
| Source authority | Fresh boundary ID is retained outside the candidate before computation. Saved verification reads an explicitly supplied retained anchor key, never the candidate's own expected ID; accepted namespace-wide source replay remains mandatory. |
| Exact scientific result | Accepted Analysis reconstruction precedes projections. Original root bytes and uncertainty seeds are preserved. Rehashed missing/extra/mixed/tampered material is rejected. |
| Populations and Coverage | Original capture-stage validation supplies captured membership independently of derivation/scoring. Full reconciliation and unknown dates remain visible. Historical offered rates are withheld for unknown membership; conflicting mapping fails. |
| Reference/calibration | Same exact Measurement IDs as each performance; approved 0.5 reference only; fixed 50-digit arithmetic and original bin endpoints/WACE. No new scoring, uncertainty or significance rule. |
| Chronology | Source K and fixed event scopes stay independent of actual computation; added projection/render/package times follow the original report. Later verification dates belong only to new receipts. |
| Immutability | Candidate validation precedes package/reference publication; prior source and report bytes are tested unchanged, including after later correction/completion. Open reads saved output only. |
| Local workflow/failures | Separate generation/selection and atomic live update retain historical identity and dates. Failed writes/verification/reference changes preserve prior selections; no-prior and status-write failures are explicit. |
| Readable export | Canonical values, static structure and links pass static checks. Combined user-supplied PDF evidence satisfies the representative basic-export visual check, including expanded live details and supporting-file views. Unsampled disclosures and personally executed browser interactions remain explicit sampling limits, not new blockers. |

The code review checked the original helper extraction, independently retained-anchor
flow, reconstruction order, package inventory/identity definition, actual clocks,
separate mutable entry, output boundaries and failure paths. The result remains a
bounded local delivery mechanism; no database, service, collector integration or
new scientific semantics was required. No material product decision was silently
introduced. Full original authority and accepted limitations remain intact.

## Validation evidence and limits

Implementation-author execution: **117 focused tests passed in 22.590 seconds;
893 full repository tests passed in 61.768 seconds.** The linked logs retain exact
commands/results and expected negative-test diagnostics. The full suite includes
NFL/World Cup compatibility and original V3 serialization/identity/seed fixtures.
New tests cover the handoff's authority, replay, population, late-source, failure,
separate-selection and offline-open cases. All archives and outputs are disposable
synthetic material. No result claims real-study completeness, production fitness,
collection health, favorable performance or empirical sufficiency.

Static HTML QA passes for both saved reports and the failure-state entry. The
historical example has four scored observations including infinite log loss; the
live example separates one score, a captured unresolved opportunity, a missed
capture and a future not-yet-due obligation, with a valid empty bounded result.
The original browser tool attempt was blocked. Subsequent user-supplied PDF exports
now provide limited visible-layout evidence, as assessed in the separate [PDF visual-evidence review](</Users/tom/.codex/.chatgpt-projects/g-p-6a6cba0d8b288191820b710ce15a39e4/V1_2_DELIVERY_PDF_VISUAL_REVIEW.md>).
This task read and incorporated that assessor's report; it did not independently
rerender the PDFs or exercise a browser.

### PDF evidence amendment — September 13, 2026

The coordinating task rendered and inspected every page of Saved MLB reports.pdf,
Historical candle report.pdf and Performance Report.pdf, examined text/link
annotations and compared representative saved projection populations. The
[implementation receipt](MLB_REPORTING_DELIVERY_IMPLEMENTATION_v1.2.0.md) records
the exact source PDF hashes and evidence provenance.

Default visible headings, tables, long identifiers and Decimal values showed no
clipping, overlap or missing glyphs. Synthetic/study/status labels, visible
chronology, historical four-observation results, live failure-inclusive populations,
valid-empty bounded output, and the entry's preserved failed-update references
agree with the retained packages. PDF link annotations identify expected local
package/supporting files; this establishes intended targets, not successful clicks.
No concrete rendering defect requiring code changes was found in that visible
content. This is limited visual evidence, not independent code acceptance.

### Subsequent PDF batch — representative visual requirement satisfied

The Owner supplied Performance Report_2.pdf, Original.pdf, projections.pdf,
source.pdf and REPRODUCE.pdf. The coordinating visual assessor rendered and
inspected all five complete pages in consecutive segments. Expanded live cumulative
calendar/identity/limitations and scientific provenance wrap readably without
clipping or overlap. The four supporting exports match retained live payload text
when export whitespace is ignored; this is a content comparison, not canonical
byte-integrity verification or scientific replay. Exact PDF hashes are recorded
in the implementation receipt and supplied evidence review.

Together with the first batch's default historical/live and failed-update entry
views, this satisfies the **representative basic-export visual requirement**.
No code correction is indicated. This supersedes the earlier request for additional
exports and personally executed navigation evidence as prerequisites to this
representative check. No further screenshots are requested absent a concrete defect.

Expanded bounded and historical disclosure instances remain unsampled. The assessor
did not personally execute disclosure toggling or browser navigation; supporting
views are consistent with accessible intended link targets, not proof of every
click. Narrow viewports, assistive technologies and paper pagination remain untested.
These are proportionate sampling limits for the approved basic export, not a new
comprehensive browser/accessibility gate. Existing static link/structure checks and
automated offline-open tests remain separate evidence. The browser security
restriction was not bypassed.

This task read and incorporated the coordinating assessor's disposition; it did
not independently render the PDFs, execute browser interactions or rerun unchanged
tests. The evidence completes the visual requirement only, not independent code
review, production report validation, integration or release acceptance.

## State changed and next gate

Only local candidate code/docs, planning receipts and disposable synthetic
archives/packages were written. Git fetch updated local tracking metadata. No
provider call, real archive/index/checkpoint/Evidence mutation, collector lock/job/
scheduler change, production report generation/publication, commit/push/PR/merge,
deployment, tag or closure occurred. Source and Analysis libraries remain unchanged;
only two original private validation helpers were extracted in legacy research.

This amendment changed only validation documents/receipts. The five implementation
and test file hashes still match the 117-focused/893-full-suite candidate. Source
PDFs, supplied review, code, packages and source archives remain unchanged.

Next: a reviewer independent of the implementation author must inspect the exact
uncommitted candidate against the approved must-hold conditions and accepted
limitations, then issue a separate independent acceptance decision with its own
validation evidence. The linked handoff specifies this gate. Local implementation authorization remains in
force for necessary bounded corrections/tests. This review confers no permission
to integrate, deploy, publish real reports, acquire data or close a study. The
polished reader, commissioning, original closure/final obligations and release
readiness remain future gates. No forward deployment or release handoff is supplied.
