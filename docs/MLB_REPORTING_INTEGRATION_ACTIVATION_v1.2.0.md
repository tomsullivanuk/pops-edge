# Codex handoff — reporting integration and local display activation

Owner authorization explicitly covers complete reporting commit/push/PR/merge and
local activation, including original accepted delivery commit
`20c1a009c59dd88c5796e933adc5b62adf509195` and subsequent readability changes. No
repeat permission is required for this sequence. Repository:
https://github.com/tomsullivanuk/pops-edge. Refreshed main/base:
`3001ee9cd4d95519567e0e4567ff71be0465a6a0`. Reconcile any subsequent main changes.

Apply Methodology > Product > Architecture > reporting contract and latest Owner
display decisions. Read [original independent acceptance](MLB_REPORTING_INDEPENDENT_REVIEW_v1.2.0.md)
and [bounded amendment/integration review](MLB_REPORTING_AMENDMENT_REVIEW_v1.2.0.md).
The original review remains accepted; do not reopen deferred science/resilience work.

## Must hold

Preserve exact scientific arithmetic, source/Analysis/projection contracts and
v1/v2 byte replay; v3 shows at most two decimals with honest ties, human-readable
calendar/report/update/all Details, visible missingness/failure/uncertainty, and
raw evidence as downloads. New display dates never replace original scientific
times. Normal-entry activation preserves identical parseable selection/last-attempt
state and old immutable packages/anchors/receipts. A backup and guarded rollback
suffice; no server or generalized publication subsystem is required.

## Integration

Review the full PR inventory against main, not just the second amendment. Commit
only intended code/tests/governing docs/review/handoff; exclude all planning scratch,
logs, generated output and runtime source. Run proportionate focused and full
compatibility checks, record actual results, push the accepted branch, create a
correctly scoped PR, inspect its exact head/base/checks, and merge the accepted head.
Do not claim hosted CI if none exists. Update task naming with the actual PR number.

## Local activation

Create a clean separate reporting checkout at the exact merged main revision.
Do not repin the collector checkout. Use the documented `activate-display` command
against `/Users/tom/PopsEdgeReports/mlb/real/2026-09-13-accepted/reports` with that
explicit revision. The existing selected historical/live packages and independent
retained anchors/verification receipts are the only result inputs. No expensive
new scientific run is required for this formatting activation.

Verify actual `entry.html` digest equals the prepared activation record, read-entry
state exactly matches its prior value, new historical/live/evidence links resolve,
all readable disclosures meet formatting requirements, all original files except
the explicitly mutable entry retain their bytes, and the executing checkout is clean.
Return the normal active entry prominently, historical link, PR/merge revision,
independent decision, local checks and activation/rollback receipt. An already-open
older page may need reopening; immutable saved package pages intentionally remain
frozen. Do not claim observed browser interaction or bypass the recorded local
browser restriction.

If rollback is needed, use `rollback-display` with the activation ID and same clean
revision. It refuses a later changed current entry or altered backup. Rollback affects
only the mutable display; no scientific record is rewritten.

## Exclusions and accepted limitations

No provider calls/new acquisition, source/archive/index/checkpoint mutation,
collector repin/restart, scheduler changes, cloud/shared NFL-MLB shell, study closure,
Policy/wagering or whole-v1.2 release/tag. Early capture gaps remain visible accepted
history, not a new activation prerequisite. Local trusted files/clocks, manual
operation, retained source requirements for future scientific replay and visible
non-authoritative failure remain accepted. Prior independent acceptance, actual
saved-package verification and focused amendment review are reused proportionately.
